# @blakfy/example-react-vite

Vite + React 18 demo — `@blakfy/accessibility-widget-react` entegrasyonu (`<A11yWidget />` + `useA11yPreferences`).

## Çalıştırma

```bash
pnpm install
pnpm --filter @blakfy/example-react-vite dev
```

http://localhost:3001
